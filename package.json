{
  "name": "world-cup-forecast-lab",
  "version": "0.3.0",
  "private": true,
  "description": "World Cup match forecast using Elo, Dixon-Coles and Polymarket data.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check public/app.js && node --check server.js && node --check worker.mjs",
    "deploy:cloudflare": "wrangler deploy"
  },
  "devDependencies": {
    "netlify-cli": "^23.0.0",
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
